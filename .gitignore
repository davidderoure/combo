__pycache__/
*.pyc
.venv/
.DS_Store
.pytest_cache/

# Trained model weights (ensemble/sax.py, DESIGN.md §12) — kept out of the public
# repo both because they're a binary build artifact and because they're derived
# from training on the Weimar Jazz Database, which carries its own research-use
# terms. Copy in manually from ~/wolfson/models/sax_best.pt — see README.
ensemble/wolfson/models/

# rehearsal_ab_test.py --plot output -- a regenerable chart, not source.
rehearsal_ab_test*.png

# self_test.py --persist (Phase 26) -- personal practice data, not source.
rehearsal_memory/

# Weimar Jazz Database (Phase 28, DESIGN.md §13) -- external research data
# downloaded from jazzomat.hfm-weimar.de, not source; also a derived
# frequency-table cache built from it. Never redistributed via this repo.
wjd_data/
